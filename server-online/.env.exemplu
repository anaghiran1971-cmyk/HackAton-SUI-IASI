PORT=8080
CORS_ORIGINI=http://localhost:5173,http://localhost:3000

# Sui RPC
SUI_RPC_URL=https://fullnode.testnet.sui.io:443
SUI_RETEA=testnet

# LLM local (ex: Ollama)
LLM_URL=http://localhost:11434
LLM_MODEL=llama3.1:8b-instruct

# Optional: protecție minimă pentru API (recomandat dacă îl pui public)
CHEIE_API_SERVER=schimba-ma

# Optional: Redis pentru cache
REDIS_URL=redis://localhost:6379

# Optional: Postgres pentru persistenta
DATABASE_URL=postgresql://copilot:copilot@localhost:5432/copilot_sui

# Optional: webhook pentru worker notificari
WEBHOOK_NOTIFICARI=

